# Secure Vault
this project uses custom MULTISBOX  block ciphher of 256 bit block length for encryption and decryption in ECB mode
and stores files in encrypted format
the 32 byte key is derived from user password by SHA256 hashing

